// loading component helps in showing loading before data gets loads or fetch just simply create a file name loading and write your code you can show anything as loaing like skeleton,image or text
import React from "react";

const loading = () => {
  return <div>loading....</div>;
};

export default loading;
